{
  "display_name": "situations",
  "description": "A LastDB app for active operational posture (situations, phases/sets, scope, links, preflight policy) and non-blocking agent-impact notices (upgrade/restart FYI timeline). The existing fsituations/* schema namespace remains compatible.",
  "homepage_url": "https://github.com/EdgeVector/situations",
  "host_track": {
    "app": "situations",
    "install_mode": "artifact",
    "kind": "artifact-bundle",
    "command": "situations",
    "aliases": [
      "fsituations"
    ],
    "gate": "lastgit",
    "gate_main": "lastdb:///situations#main",
    "artifact_app": "situations",
    "artifact_channel": "stable",
    "artifact_root": "$HOME/.lastgit/artifacts",
    "install_root": "$HOME/.host-track/apps/situations",
    "links": [
      {
        "source": "dist/situations",
        "target": "$HOME/.local/bin/situations"
      },
      {
        "source": "dist/fsituations",
        "target": "$HOME/.local/bin/fsituations"
      }
    ],
    "notes": "Verified LastGit artifact install; PATH links point into the immutable Host Track artifact bundle. Upgrade with host-track refresh situations; never develop in the install tree."
  }
}
